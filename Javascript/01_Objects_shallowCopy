console.log("Hello world");

const user = {
    name : "Sahil",
    age : 22,
    emailId : "sahil01@gmail.com",
    fees : 2000,
    address : {
        city : "Pune",
        State : "Maharashtra"
    }
}

//const user2 = {...user};

const user2 = structuredClone(user);
user2.address.city="Chennai";

console.log(user2);
console.log(user);

for (const[key,value] of Object.entries(user)){
    console.log(`${key} : ${value}`);
}
